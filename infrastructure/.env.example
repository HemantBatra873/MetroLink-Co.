# Copy to infrastructure/.env for local overrides (do not commit .env).
POSTGRES_USER=metrolink
POSTGRES_PASSWORD=metrolink_dev
POSTGRES_DB=postgres
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
